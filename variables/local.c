#include <stdio.h>
/**
 * main - declares local variables a, b and c
 * Return: 0
 */
int main(void)
{
	int a = 10, b = 20, c;

	c = a + b;
	printf("The value of a = %d, b = %d and a + b = %d\n", a, b, c);
	return (0);
}
